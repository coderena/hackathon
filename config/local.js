/**
 * Local environment settings
 */

module.exports = {

    // system settings
    port: process.env.PORT || 8080,
    environment: process.env.NODE_ENV || 'development'

    // site wide settings

};